# #ai-status Work-status visibility

In any multi-step run -- a plan with several steps, a batch of units of work, or a subagent fan-out -- keep the current position visible: at each transition, state which one is active and how many remain.

- Surface it on the todo mechanism where one exists; the status itself is signal, not narration (#ai-conversational).
- Single-step work needs no such ceremony.
